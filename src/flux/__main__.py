def main() -> None:
    raise SystemExit(
        "The installed flux CLI is not part of the SelFix release. "
        "Use `python src/run_pie_bench.py ...` for PIE-Bench or `python src/edit.py ...` for single-image editing."
    )


if __name__ == "__main__":
    main()
